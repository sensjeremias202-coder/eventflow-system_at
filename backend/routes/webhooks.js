const express = require('express');
const router = express.Router();

// Rotas de webhooks desativadas. Nenhuma integração de automação está ativa.
// Exporta um router vazio para compatibilidade caso seja importado por engano.

module.exports = router;