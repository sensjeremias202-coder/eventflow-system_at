# Eventflow Backend - Exemplo de configuração

# Porta do servidor
PORT=5000

# MongoDB
# Use Atlas (recomendado) ou local: mongodb://127.0.0.1:27017/eventflow
MONGODB_URI=mongodb://127.0.0.1:27017/eventflow

# JWT (use um segredo forte)
JWT_SECRET=troque-por-um-segredo-forte

# URL do frontend (para links de email de recuperação)
FRONTEND_URL=http://127.0.0.1:5500

# SMTP (opcional). Se não configurar, os emails serão logados no console.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# Remetente de emails
MAIL_FROM=no-reply@eventflow.local
