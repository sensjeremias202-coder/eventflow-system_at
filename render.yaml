services:
  - type: web
    name: eventflow-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    plan: free
    envVars:
      - key: PORT
        value: 5000
      # Defina no painel da Render os segredos abaixo (recomendado usar "fromSecret")
      # - key: MONGODB_URI
      #   fromSecret: MONGODB_URI
      # - key: JWT_SECRET
      #   fromSecret: JWT_SECRET
      # - key: CORS_ORIGIN
      #   value: https://sensjeremias202-coder.github.io
      # - key: FRONTEND_URL
      #   value: https://sensjeremias202-coder.github.io/eventflow-system_at
